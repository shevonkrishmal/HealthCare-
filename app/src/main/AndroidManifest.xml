<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.madproject">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
       <!-- <activity android:name=".MADMAIN"></activity> -->
       <!-- <activity android:name=".mad4" /> -->
        <activity
            android:name=".MAD3"
            android:label="@string/title_activity_m_a_d3">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MAD1">
          <!--  android:label="@string/title_activity_m_a_d3"> -->
            <intent-filter>
                <action android:name=".MAD1" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".Mad2" >
           <!-- android:label="@string/title_activity_m_a_d3"> -->
            <intent-filter>
                <action android:name=".Mad2" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".MADMAIN" >
           <!-- android:label="@string/title_activity_m_a_d3"> -->
            <intent-filter>
                <action android:name=".MADMAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".mad4" >
            <!--android:label="@string/title_activity_m_a_d3"> -->
            <intent-filter>
                <action android:name=".mad4" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

     <!--   <uses-library
            android:name="com.google.android.wearable"
            android:required="true" /> -->
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
       <!-- <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" /> -->

      <!--  <activity android:name=".Mad2" />
        <activity android:name=".MAD1" /> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name=".MainActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>